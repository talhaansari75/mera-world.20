import { createFileRoute } from "@tanstack/react-router";
import { useEffect,useState } from "react";
import { connectWallet,sendUsdcPayment,switchToChain } from "@/lib/web3/base";
type Product={id:string;name:string;priceUsd:string;amountAtomic:string;diamonds:number}; type Config={chainId:number;tokenAddress:string;recipientAddress:string;products:Product[];error?:string};
export const Route=createFileRoute("/shop")({component:Shop});
function Shop(){const [config,setConfig]=useState<Config|null>(null),[wallet,setWallet]=useState(""),[busy,setBusy]=useState(false),[message,setMessage]=useState("");
useEffect(()=>{fetch("/api/payments/base").then(r=>r.json()).then(setConfig).catch(()=>setMessage("Payment service unavailable."))},[]);
async function buy(item:Product){if(!config)return;setBusy(true);setMessage("");try{const address=await connectWallet();setWallet(address);await switchToChain(config.chainId);const ir=await fetch("/api/payments/base",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"create",productId:item.id,payerAddress:address})});const intent=await ir.json();if(!ir.ok)throw new Error(intent.error||"Could not create payment.");const txHash=await sendUsdcPayment(intent.tokenAddress,intent.recipientAddress,intent.amountAtomic);setMessage("Transaction submitted. Waiting for confirmations…");for(let attempt=0;attempt<24;attempt++){await new Promise(r=>setTimeout(r,5000));const vr=await fetch("/api/payments/base",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"verify",intentId:intent.intentId,txHash})});const d=await vr.json();if(d.status==="paid"){setMessage(`Payment confirmed. ${item.diamonds.toLocaleString()} gems unlocked.`);return}if(vr.status!==202)throw new Error(d.error||"Payment verification failed.")}setMessage("Payment is still confirming. You can leave this page safely; the listener records blockchain events.")}catch(e){setMessage(e instanceof Error?e.message:"Payment failed.")}finally{setBusy(false)}}return <main className="min-h-dvh bg-[#060914] px-4 py-10 text-white"><div className="mx-auto max-w-5xl"><h1 className="text-4xl font-semibold">Gem Shop</h1><p className="mt-2 text-slate-400">Secure USDC checkout with server-side blockchain verification.</p>{wallet&&<p className="mt-3 text-xs text-slate-500">Wallet: {wallet.slice(0,6)}…{wallet.slice(-4)}</p>}{!config?<p className="mt-8">{message||"Loading payment options…"}</p>:config.error?<div className="mt-8 rounded-2xl p-5">{config.error}</div>:<div className="mt-8 grid gap-5 md:grid-cols-3">{config.products.map(item=><article key={item.id} className="rounded-3xl border border-white/10 bg-white/[0.05] p-6"><h2 className="text-xl font-semibold">{item.name}</h2><p className="mt-4 text-3xl font-bold">${item.priceUsd}</p><p className="mt-2 text-slate-400">{item.diamonds.toLocaleString()} Gems</p><button disabled={busy} onClick={()=>buy(item)} className="mt-6 w-full rounded-2xl bg-indigo-500 px-4 py-3 font-semibold disabled:opacity-50">{busy?"Confirming…":"Pay with USDC"}</button></article>)}</div>}</div></main>}
