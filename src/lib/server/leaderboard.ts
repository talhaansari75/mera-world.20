import { createServerFn } from "@tanstack/react-start";
import { getPrisma } from "@/lib/db";
import { authMiddleware } from "@/lib/auth/middleware";
const BOARDS = new Set(["stars", "daily", "words"]);
export const getLeaderboard=createServerFn({method:"GET"}).validator((board:string)=>(BOARDS.has(board)?board:"stars")).handler(async({data:board})=>getPrisma().leaderboardScore.findMany({where:{board},orderBy:[{score:"desc"},{createdAt:"asc"}],take:25,select:{displayName:true,score:true,createdAt:true}}).then(rows=>rows.map(r=>({...r,createdAt:r.createdAt.toISOString()}))));
export const submitScore=createServerFn({method:"POST"}).middleware([authMiddleware]).validator((d:{sessionId:string;board:string})=>({sessionId:String(d.sessionId??"").slice(0,64),board:BOARDS.has(d.board)?d.board:"stars"})).handler(async({context,data})=>{const s=await getPrisma().gameSessionV5.findUnique({where:{id:data.sessionId}});if(!s||s.userId!==context.userId||s.status!=="completed")return{ok:false as const,error:"Only a completed verified gameplay session can enter the leaderboard."};return{ok:true as const,message:"Verified result already recorded by the server."};});
export const submitDaily=createServerFn({method:"POST"}).middleware([authMiddleware]).validator((d:{sessionId:string})=>({sessionId:String(d.sessionId??"").slice(0,64)})).handler(async({context,data})=>{const s=await getPrisma().gameSessionV5.findUnique({where:{id:data.sessionId}});if(!s||s.userId!==context.userId||s.status!=="completed")return{ok:false as const,error:"Only a completed verified daily session can enter the leaderboard."};return{ok:true as const,message:"Verified daily result already recorded by the server."};});
export const getDailyBoard=createServerFn({method:"GET"}).validator((day:string)=>String(day??"").slice(0,16)).handler(async({data:day})=>getPrisma().dailyResult.findMany({where:{dayKey:day},orderBy:[{score:"desc"},{timeMs:"asc"}],take:25,select:{displayName:true,score:true,timeMs:true,stars:true}}));
