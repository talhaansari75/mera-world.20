import { getPrisma } from "@/lib/db";
export async function dashboardSnapshot(){const db=getPrisma();const [users,events,rooms,reports,purchases]=await Promise.all([db.gameplayEvent.findMany({distinct:["userId"],where:{userId:{not:null}},select:{userId:true}}),db.gameplayEvent.count({where:{createdAt:{gt:new Date(Date.now()-86400000)}}}),db.multiplayerRoom.count({where:{status:"open"}}),db.moderationReport.count({where:{status:"open"}}),db.purchaseReceipt.count({where:{status:"verified"}})]);return{usersSeen:users.length,events24h:events,openRooms:rooms,openReports:reports,verifiedPurchases:purchases}}
